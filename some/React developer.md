Обов’язково освіжи:

- створення компонентів (function components)
- props / state
- useState, useEffect
- робота зі списками (map)
- форми (onChange, onSubmit)
- lifting state up
- умовний рендеринг
- робота з API через fetch / axios (GET, POST)
    

❗ Junior позиції майже завжди дають:  
**“Зроби компонент / список / форму / фільтрацію / пошук / трохи асинхронщини”.**




## 2. **Типові завдання на live coding**

### ✔ Список із фільтрацією

Наприклад:

> Є масив users. Зроби пошук, сортування (A-Z), видалення юзера.

### ✔ Форма створення об'єкта

> Зроби форму, яка додає item у список (з валідацією).

### ✔ Робота з API

> Зроби fetch до публічного API (наприклад, todos) і виведи список.

### ✔ Компонент зі станом

> Зроби лічильник, таймер, модальне вікно — прості, але показують розуміння хуків.

---

## 🔥 3. **JS/TS, які потрібно освіжити**

- map, filter, reduce
    
- деструктуризація
    
- spread/rest
    
- async/await
    
- типізація props та state у TypeScript  
    (interfaces / type, FC, тип подій, типи API)
    

---

## 4. **Redux або Apollo**
Redux:

- створення slice (Redux Toolkit)

`createSlice` — це утиліта з Redux Toolkit (RTK), що об'єднує **initial state**, **reducers** і **action creators** в одному місці. Замість ручного прописування типів дій, switch/case і окремих action creators — ти отримуєш компактний, декларативний і безпечний код.
# Навіщо `slice`?

- Менше шаблонного коду (boilerplate).
- Генерує action creators автоматично.
- Працює з Immer — можна писати "мутаційний" код, але під капотом все іммутабельно.
- Простота тестування і читання.



- useDispatch, useSelector
- базова логіка: додати/видалити/змінити

### Apollo:
- useQuery
- useMutation

## 🔥 5. **Якщо дадуть міні-проєкт**

З тексту вакансії видно, що компанія працює з:
- React + TypeScript
- Redux або Apollo
- REST або GraphQL
    

### На що дивитись першим у проєкті:

1. **Структура папок:**
    - /components
    - /pages
    - /hooks
    - /api
    - /store
2. **Назви файлів та компонентів** — чітко та зрозуміло.
3. **Чистий код:**
    - видалити зайве
    - лаконічність
    - уникати дублювання
4. **Типізація**
    - обов’язково типізувати props
    - тип відповіді API
    - тип item у списку
5. **Управління станом**
    - якщо дадуть Redux → тримай логику в slice
    - якщо без Redux → піднімаємо state вверх або робимо custom hook
6. **Працюючий UI без дизайну**  
    Вони не очікують красу — вони очікують **логіку**.
7. **Обробка помилок**
    - try/catch для fetch
    - loading і error стани

# 🎯 6. **Що точно НЕ перевірятимуть у джуна**

- складний useMemo, useCallback
- оптимізацію рендерів
- Webpack, Babel
- власні кастомні хук-форміки
- unit-тести