## **1. Що таке тег `<label>`?**

**`<label>`** — це HTML-тег, який використовується для **підпису елементів форми** (`input`, `textarea`, `select`).  
Його основна мета — **покращення доступності та зручності використання форм**.
### Основні переваги:
- користувач розуміє, для чого поле (ім’я, email, пароль);
- клік по тексту `<label>` активує пов’язаний `input`;
- покращує accessibility (екранні читачі).
###Варіанти використання:

#### 1️⃣ Через атрибут `for`
`<label for="username">Імʼя користувача</label> <input id="username" type="text">`
#### 2️⃣ Обгортаючи `input` всередину `label`
`<label>   Приймаю умови   <input type="checkbox"> </label>`

---
## **2. Для чого використовується тег `<meta>`?**
Тег `<meta>` містить **метаінформацію про сторінку**, яка не відображається напряму користувачу, але важлива для браузера та пошукових систем.
### Основні приклади:
- **Кодування сторінки**
`<meta charset="UTF-8">`
- **Адаптивність і масштабування**
`<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- **Опис сторінки (SEO)**
`<meta name="description" content="Опис сторінки, який бачать користувачі в пошукових системах">`
---
## **3. Що таке `DOCTYPE` і для чого він потрібен?**
`<!DOCTYPE html>` — повідомляє браузеру, **яку версію HTML використовувати**.
- вказує, що документ написаний на **HTML5**;
- запобігає некоректному рендерингу;
- має бути **першим рядком HTML-документа**.
`<!DOCTYPE html>`
---
## **4. Різниця між `<button>`, `<input type="submit">`, `<input type="button">`, `<input type="reset">`**

### `<button>`
- універсальна кнопка;
- може містити **HTML, іконки, текст**;
- має тип за замовчуванням `submit`.
`<button type="button">Звичайна</button> <button type="submit">Відправити</button> <button type="reset">Скинути</button>`
### `<input type="submit">`
- кнопка **відправки форми**;
- текст задається через `value`;
- не може містити HTML.
`<input type="submit" value="Відправити">`
### `<input type="button">`
- звичайна кнопка;
- не відправляє форму.
### `<input type="reset">`
- скидає всі поля форми до початкових значень.
---

## **5. Що таке `object-fit`?**
`object-fit` — це CSS-властивість, яка визначає, **як зображення або відео вписується в контейнер**.
Використовується для:
- `<img>`
- `<video>`
- `<object>`
`img {   object-fit: cover; }`

### Значення:
- **fill** — розтягує елемент (можливе спотворення)
- **contain** — зберігає пропорції, весь контент видно
- **cover** — заповнює контейнер, частина обрізається
- **none** — не масштабується
- **scale-down** — вибирає менше між `none` і `contain`
---

## **6. Що таке Promise і чим він відрізняється від `async/await`?**
### Promise
`Promise` — це обʼєкт, який представляє **результат асинхронної операції**.
Стан:
- `pending`
- `fulfilled`
- `rejected`
`fetch(url)   .then(res => res.json())   .then(data => console.log(data))   .catch(err => console.error(err))`

### `async/await`

Це **синтаксичний цукор над Promise**, який дозволяє писати асинхронний код як синхронний.

async function getData() {
  try {
    const res = await fetch(url)
    const data = await res.json()
    console.log(data)
  } catch (err) {
    console.error(err)
  }
}

`async/await` **працює поверх Promise**, а не замінює його.

---

## **7. Різниця між `null`, `undefined`, `NaN`**
- **`undefined`** — змінна оголошена, але значення не присвоєно
- **`null`** — явна відсутність значення (задає програміст)
- **`NaN`** — результат некоректної математичної операції
`let a let b = null let c = 5 / "text"`
---
## **8. Чим Context відрізняється від Redux?**
### Context API
- для передачі даних між компонентами;
- підходить для **невеликого стану** (тема, мова, авторизація);
- немає складної логіки.
### Redux
- глобальне керування станом;
- строгі правила (store, actions, reducers);
- підходить для **великих додатків**;
- легше дебажити.

Redux = масштабованість  
Context = простота

---

## **9. Як працює `key` у React?**

`key` — це **унікальний ідентифікатор елемента списку**, який допомагає React ефективно оновлювати DOM.
`items.map(item => (   <li key={item.id}>{item.name}</li> ))`
Погано:
`key={index}`
Добре:
`key={item.id}`

---
Якщо хочеш — можу:
- скоротити це до **коротких відповідей для усної співбесіди**
- або зробити **шпаргалку на 1 сторінку**
- або **перевірити тебе питаннями як інтерв’юер** 💼